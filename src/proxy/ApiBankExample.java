package proxy;

/**
 * Abstracion generica de ejemplo simplificado para simular una API para
 * conectar a una cuenta bancaria
 */
public class ApiBankExample {
    public static void withdraw(String id, String password, Integer amount, String idToAccount) {
    }

    public static Integer getBalance(String id, String password) {
        return 100000;
    }
}
